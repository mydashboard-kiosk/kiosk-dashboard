Drop your slideshow images into this folder:
  /photos

Then update photos.json to list them, for example:
{
  "files": ["001.jpg", "family.png", "camp.webp"]
}

GitHub Pages cannot auto-list folders, so the manifest is required.
